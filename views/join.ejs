<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Join Clubhouse</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
	<%-include('./partials/navbar')%>
	<% if (errors && errors.length > 0) { %>
		<ul style="color: red;">
		  <% errors.forEach(error => { %>
		    <li><%= error.msg %></li>
		  <% }) %>
		</ul>
	<% } %>


	<%# ADD PASSWORD TABLE USE BYCRPT TO BECOME ADMIN \ %>
	<div class="text-center mx-auto text-4xl py-8 ">Join Clubhouse</div>	
	<div class="text-center mx-auto text-xl py-8 font-bold ">Enter the Secret code inorder to be a Clubhouse Member</div>	
	<form  method="POST" action="/join" class="max-w-md mx-auto p-6 bg-white shadow-md rounded-lg space-y-4">
		<div>
		    <label class="block text-sm font-medium text-gray-700">Password</label>
		    <input 
		      type="password" 
		      name="pw" 
		      class="mt-1 w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
		    />
	  </div>
  	    <button 
	      type="submit" 
	      class="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 transition cursor-pointer" 
	    >
	      Join Club
	    </button>
	</form>

</body>
</html>